using UIKit;

namespace Wardkitten.Mobile;

public class Program
{
    // Punto de entrada de la app iOS (requiere macOS + Xcode para compilar).
    private static void Main(string[] args) => UIApplication.Main(args, null, typeof(AppDelegate));
}
