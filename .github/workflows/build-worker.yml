name: Build Worker

on:
  push:
    branches: [main, Release]
    paths:
      - 'src/Wardkitten.Worker/**'
      - 'src/Wardkitten.Application/**'
      - 'src/Wardkitten.Infrastructure/**'
      - 'src/Wardkitten.Domain/**'
      - 'src/Wardkitten.Shared.Contracts/**'
      - '.github/workflows/build-worker.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Worker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: src/Wardkitten.Worker/Dockerfile
          push: true
          tags: |
            ghcr.io/nimitaco/wardkitten-worker:${{ github.run_number }}
            ghcr.io/nimitaco/wardkitten-worker:latest
          # Credencial del feed NimitaCo como secreto de BuildKit (el Dockerfile la monta en el
          # restore; con NIMITACO_NUGET_TOKEN sin crear, el Password queda vacío y se ignora).
          secrets: |
            nimitaco_nuget=Username=${{ github.actor }};Password=${{ secrets.NIMITACO_NUGET_TOKEN }}

      - name: Fijar imagen Worker en el manifiesto y desplegar (ArgoCD)
        run: |
          set -euo pipefail
          tag="${{ github.run_number }}"
          file="K8S/produccion/worker.yaml"
          sed -i -E "s#^([[:space:]]*image: ghcr\.io/nimitaco/wardkitten-worker:).*#\1${tag}#" "$file"
          if git diff --quiet -- "$file"; then
            echo "El manifiesto ya apunta a :${tag}; nada que commitear."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$file"
          git commit -m "K8S deploy wardkitten-worker:${tag} [skip ci]"
          git push origin HEAD:${{ github.ref_name }}
